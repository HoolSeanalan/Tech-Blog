<article class="post">
  <div class="title">
    <a href="/post/{{id}}">{{title}}</a>
    <div>{{body}}</div>
  </div>
  <div class="meta">
    By {{user.username}} on {{formatDate created_at}}
    {{comments.length}} {{formatPlural "Comment" comments.length}}
  </div>
</article>