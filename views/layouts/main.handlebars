<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>BlogIt - Tech</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">BlogIt - Tech</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a href="/dashboard">Dashboard</a>
                <button id="logout">Logout</button>
                {{else}}
                <a href="/login">Login/Signup</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>
            <p>&copy; {{ 2021 }}</p>
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>